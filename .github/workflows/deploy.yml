name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install JupyterLite
        run: pip install jupyterlite-core jupyterlite-pyodide-kernel jupyter-server

      - name: Collect notebooks for JupyterLite
        run: |
          mkdir -p _jl_contents
          for ipynb in posts/*/content.ipynb; do
            [ -f "$ipynb" ] || continue
            slug=$(basename $(dirname "$ipynb"))
            mkdir -p "_jl_contents/posts/$slug"
            cp "$ipynb" "_jl_contents/posts/$slug/content.ipynb"
          done

      - name: Build JupyterLite
        run: jupyter lite build --contents _jl_contents --output-dir lab --base-url /pages/lab/

      - name: Prepare site
        run: |
          mkdir -p _site
          # Static site files (루트에 있는 파일/폴더 복사)
          for item in index.html style.css app.js post.js graph.js sidebar.js page-fold.js \
                      blog posts github projects.json github-sources.json .nojekyll; do
            [ -e "$item" ] && cp -r "$item" _site/ || true
          done
          # JupyterLite 출력
          cp -r lab _site/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '_site'

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/deploy-pages@v4
        id: deployment
